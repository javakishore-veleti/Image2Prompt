export const environment = {
  production: false,
  // API gateway base URL. Customer endpoints are under /api/customer and
  // image endpoints under /api/images.
  gatewayUrl: 'http://localhost:8000',
};
